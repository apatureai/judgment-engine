{
  "name": "@engine/redis",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Engine orchestrator hot paths: resilient Redis client + key namespaces + eviction guard (token-buckets/quotas only; the job store is Postgres).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "test": "vitest run" },
  "dependencies": {
    "ioredis": "^5.11.1"
  }
}
