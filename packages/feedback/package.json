{
  "name": "@engine/feedback",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Engine feedback: explicit/implicit signal capture, per-repo memory, rater down-weighting, preference export.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "test": "vitest run" },
  "dependencies": {
    "@engine/db": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.0"
  }
}
