{
  "name": "@engine/eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Eval harness: synthetic canaries, golden-set metrics, regression + quality gate, model/prompt registry.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "test": "vitest run" },
  "dependencies": {
    "@engine/db": "workspace:*",
    "@engine/types": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.0"
  }
}
