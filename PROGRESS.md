# Apature Judgment Engine - Build Progress (loop source of truth)

Deterministic checklist for the autonomous build loop. Work top-down by milestone (EM0 -> EM6).

How the loop uses it:
- Pick the FIRST unchecked `[ ]` issue whose dependencies are already `[x]`.
- When done (code + tests green, pushed): change `[ ]` to `[x]` and append ` -> done: <one line>`.
- If blocked or it needs LIVE infra/keys (Firecracker fleet, DashScope keys, GPUs), mark `[~]` and append ` -> skipped: <reason>`, then take the next unblocked issue. Stub the model/sandbox where possible and keep going.


## EM0 · Foundation & ops

- [x] #1 - Infra: monorepo scaffold + shared critique() interface package -> done: pnpm workspace (types/capture/critique) + tsc -b project refs, strict TS, ESLint, Vitest, CI; @engine/types is the single source for critique()/captureInSandbox() + Finding/Critique/version-stamp types; @engine/capture + @engine/critique consume it (stubs; EM1/EM2 implement). Golden wire fixture copied from Gate as the cross-repo contract anchor (x-schema-version=1). typecheck+test(5)+lint green.
- [x] #2 - Infra: lint + typecheck CI pipeline -> done: GitHub Actions runs eslint + tsc + vitest on PR/push (all workspaces via `eslint .` + `tsc -b` project refs); pnpm store cached via setup-node; install is `--frozen-lockfile`; added least-privilege `permissions: contents: read`. Any error fails the check.
- [~] #3 - Infra: Fly.io app + Machines provisioning -> skipped: needs a live Fly account + Dockerfiles for the orchestrator/capture services that aren't built yet (EM0 #64 / EM1 #22). The deployable seam (db migrate as Fly release command) is in place via `@engine/db` CLI; revisit fly.toml once those services have runnable images.
- [x] #4 - Infra: Neon Postgres provisioning + migration tooling -> done: `@engine/db` deterministic up/down migration runner (paired `NNNN_name.up.sql`/`.down.sql`, `schema_migrations` tracking, idempotent apply + reverse-order rollback) tested against embedded PGlite (apply/idempotent/rollback round-trip); `migrate` CLI (`up`/`down [n]`) for the Fly release command + ephemeral Neon branch in CI; pgExecutor documents Neon pooled-endpoint usage for serverless pooling. Baseline 0001_init adds the shared `touch_updated_at()` trigger fn. Live Neon project/branch provisioning stays ops.
- [x] #5 - Infra: Redis provisioning for BullMQ + token-bucket -> done: `@engine/redis` shared resilient ioredis connection helper (maxRetriesPerRequest:null, capped retry, READONLY reconnect) + reserved key namespaces (`tb:` model-endpoint token-bucket #36, `quota:`/`pq:` per-tenant quota+priority #67, `cb:` circuit-breaker) + `assertNoEviction` startup guard documenting the required `noeviction` policy. Per the E1 architecture review the durable job store is Postgres+pg_notify (#65), NOT Redis, so namespaces are fairness/rate-limit only. Live managed Redis instance stays ops.
- [x] #6 - Infra: S3 + R2 buckets + on-demand signed-URL service -> done: `@engine/storage` — `ObjectStore` interface + `objectKey(jobId,kind,name)`/`jobPrefix` (artifacts namespaced under `jobs/<jobId>/`), `InMemoryObjectStore` fake, `DualWriteObjectStore` (R2 primary + S3 secondary; reads/signs from primary for zero-egress), and `S3ObjectStore` (S3-API adapter that also serves R2 via endpoint) with on-demand short-TTL `signedGetUrl` via an injected presigner. Signed URLs are minted per request, never persisted. Tested against fakes (no network). Live bucket provisioning stays ops.
- [x] #7 - Infra: AWS KMS per-tenant key + per-repo data-key envelope helper -> done: `@engine/secrets` — `KmsKeyProvider` seam + `LocalKms` (HKDF per-`cmkId` CMK from a root key; dev/test) and `sealForRepo`/`openForRepo` envelope: fresh per-repo DEK (AES-256-GCM) wrapped under the per-tenant/shared CMK, with `repoId` bound as AAD so a sealed secret only opens in its own repo scope. The CMK is the only KMS key; per-repo data keys are app-side — no per-repo KMS keys. Tests: round-trip, distinct DEK per seal, cross-repo + cross-tenant tamper rejection. Real AWS KMS binding stays ops.
- [x] #8 - Infra: OpenTelemetry tracing skeleton across stages -> done: `@engine/observability` — span taxonomy for every pipeline stage (job receive/capture/context/triage/deep/validate/persist) under one trace per job via `withSpan`; `setVersionAttributes` stamps {engineVersion,model,promptVersion,captureVersion} on critique spans (#68); W3C trace-context propagation through the job payload (`injectTraceContext`/`runWithTraceContext`) so the trace survives the queue->worker hop; `initTelemetry` wires exporter-agnostic providers + global W3C propagator; `EngineMetrics` exposes the SLO instruments (latency, hallucination-drops #72, capture-instability, queue-depth, model-rate-limited #36, prefix-cache-hit #34). Tested with in-memory span+metric exporters. Live OTLP backend wiring stays ops.
- [x] #9 - Infra: Grafana dashboards + alerts (cache-hit, queue depth, hallucination-drop) -> done: committed `observability/dashboard.json` (panels for queue depth, per-stage latency p95, warm-pool utilization, cache-hit rate, hallucination-drop, capture instability, model rate-limiting) + `observability/alerts.yaml` with the two body-mandated alerts (per-repo cache-hit-rate drop; `cache_read_input_tokens == 0`) plus latency/hallucination/queue SLO alerts. Added the `engine.model.cache_read_input_tokens` instrument to `EngineMetrics`. Test asserts the required panels + alert series are present. Live Grafana/Prometheus provisioning stays ops.
- [x] #10 - Infra: secrets management (KMS-backed app secrets) -> done: extended `@engine/secrets` — one typed `SecretStore` accessor (`EnvSecretStore` over `APP_SECRET_KEYS`, throws on missing; same interface Fly app secrets inject through #3); `sealRepoSecret`/`openRepoSecret` envelope-encrypt the per-repo `storageState` + `protectionBypass` secrets (KMS-backed via #7, kind folded into scope so kinds can't be confused); and `redact()` masks sensitive keys + signed URLs + image data so no secret value reaches logs/traces. Tests cover all three criteria. (Depends on #3 Fly provisioning [~] for the live secret source; the accessor/redaction/encryption are implemented.)
- [x] #36 - Orchestrator: global Redis token-bucket on model-endpoint limits -> done: `@engine/redis` token-bucket — pure `refillAndConsume` (time-based refill + consume + retryAfterMs), `InMemoryTokenBucket` (tests/single-process) and `RedisTokenBucket` (atomic Lua `TOKEN_BUCKET_LUA` so the global cap is correct across orchestrator instances), keyed by `modelEndpointTokenBucketKey` so one tenant's fan-out can't exhaust the DashScope/self-host endpoint. A denied consume returns `retryAfterMs` and the orchestrator leaves the job queued (never dropped) → backpressure surfaces as queue depth; this is the outer envelope that per-tenant quota/priority (#67) compose on. Tested incl. burst→deny→refill + Lua arg wiring.
- [x] #64 - Engine: async job API server (POST/GET/DELETE /jobs, HMAC, x-schema-version, depth) -> done: `@engine/api` `createJobApi` — transport-agnostic handlers: `POST /jobs` -> 202 {jobId} (409 on duplicate idempotency key), `GET /jobs/:id` polls (maps job status -> pending/running/completed/failed), `DELETE /jobs/:id` cancels. HMAC-SHA256 verify over `${ts}.${installationId}.${body}` (constant-time, optional skew window); every request scoped to the *verified* installationId (cross-tenant GET/DELETE -> 404, no disclosure). Results carry the `x-schema-version` header + `{engineVersion,model,promptVersion,captureVersion}`; `depth` routes the model (triage->qwen3-vl-flash, deep->qwen3-vl-plus) via `modelForDepth`. `processJob` worker step writes the result to object storage + completes. Tested against PGlite + in-memory storage. Real HTTP binding (Fastify/Node) is a thin ops adapter.
- [x] #65 - Engine: job store + pg_notify dispatch + idempotency -> done: migration `0002_jobs` (status/consumer/installation_id/idempotency_key UNIQUE/depth/result_pointer/timing, partial queued index, `touch_updated_at` + `notify_new_job` triggers) + `@engine/jobs` `JobStore`: idempotent `enqueue` (`ON CONFLICT DO NOTHING` + re-select, dedups across consumers), `claimNext` via `FOR UPDATE SKIP LOCKED` (no busy-poll; woken by `pg_notify` on `engine_jobs`), and complete/fail/cancel lifecycle. Status+metadata in Postgres, results referenced by `resultPointer` in object storage. Tested against PGlite incl. the NOTIFY-on-enqueue dispatch.
- [x] #66 - Engine: cancellation propagation (cancelling -> microVM kill + inference abort) -> done: migration `0003` adds the `cancelling` status; `JobStore.requestCancel` flips queued/running -> `cancelling` immediately (consumer sees intent at once via GET state `cancelling`) and `markCanceled` finalizes -> `canceled`; `CancellationCoordinator` registers an AbortSignal per in-flight job and on cancel aborts the inference stream + runs the `killSandbox` seam (real Fly Machines stop wired in #22), best-effort (swallows kill errors). The no-result invariant holds because `complete`/`fail` only act on `running` rows — a late `processJob` writes nothing once the job left running. The API DELETE does requestCancel + coordinator.cancel. Tested: immediate cancelling, no-result-written, finalize, abort+kill, best-effort teardown, cross-tenant 404.
- [x] #67 - Engine: capacity & fairness (token-bucket + per-tenant quota + priority queues) -> done: migration `0004` adds a `priority` column + priority-ordered queued index; `jobPriority` (gate-blocking 0 > gate-background 10 > other 20) is stamped on enqueue and `claimNext` orders by `(priority, created_at)` so the interactive path isn't starved. `FairnessGate` (`@engine/redis`) composes a per-tenant quota bucket (checked first) with the global model-endpoint bucket (#36, outer envelope); a denial returns `retryAfterMs` (429 + Retry-After) and the job stays queued (backpressure = queue depth, never dropped). Tested: priority claim order, tenant-quota isolation, endpoint-cap denial.
- [x] #68 - Engine: version stamping on all Findings results -> done: `buildResultMetadata` in `@engine/critique` assembles `{engineVersion, model, promptVersion, captureVersion, uiDnaVersion}` and **throws if any of the four required versions is empty** (a prompt/model change without a version bump is impossible); `critique()` stamps every result through it (model = resolved per-pass #26, captureVersion config-driven); `assertVersionStamped` is the defensive gate; `versionSpanAttributes` maps to the `engine.*` OTel keys (#8's `setVersionAttributes`). Versions source from the adapter constants now, the model/prompt registry (#71) later. Tested incl. missing-version rejection + span attrs.

## EM1 · Capture core

- [~] #11 - Capture: Playwright worker — 3 viewports at DSF 2 -> skipped: needs a live Chromium browser (can't run real Playwright capture/screenshots in CI per the no-live-infra rule). The `captureInSandbox` stub remains the seam, and the deterministic post-capture computations the real worker will call are implemented as pure functions (#19 done; #16/#18 pending). Revisit when capture runs on real infra.
- [~] #12 - Capture: page-readiness protocol (domcontentloaded -> fonts.ready -> layout-stable -> phash; never networkidle) -> skipped: the readiness sequence is live-browser orchestration (Playwright goto/wait/evaluate on a real page); can't run in CI per the no-live-infra rule. `captureInSandbox` stub is the seam. Research-enriched (see #12 comment + #83): never-networkidle confirmed, layout-stable via Layout Instability API. Revisit on real capture infra.
- [~] #13 - Capture: deterministic animation freeze + reduced-motion (re-inject post-scroll) -> skipped: applied in-page via Playwright (CSS injection + reduced-motion context + post-scroll re-inject) -> needs a live browser. Seam = `captureInSandbox` stub. Revisit on real capture infra.
- [~] #14 - Capture: auto-scroll lazy-load + infinite-scroll detection -> skipped: live-browser scrolling/lazy-load interaction; can't run in CI. Seam = `captureInSandbox` stub. Revisit on real capture infra.
- [x] #15 - Capture: phash stability gate excluding animated regions -> done: pure `stability.ts` in `@engine/capture` — `runStabilityGate(sampler, opts)` takes up to N shots (default 3) and declares stable as soon as a consecutive pair matches within BOTH a phash and an orthogonal structural-diff hash threshold (architecture review E4); `hammingDistance`/`hashesWithin` over hex hashes; on persistent instability returns the last shot + `UNSTABLE_CONFIDENCE_CEILING` (feeds #70). Animated-region exclusion is applied by the sampler via #18 `animatedExclusions` before hashing. Sampler/hashing/masking is the worker seam; gate logic is pure + tested.
- [x] #16 - Capture: screenshot downscale to model pixel budget + coordinate rescale -> done: pure `downscale.ts` — `fitToPixelBudget`/`fitForDepth` shrink a tile under the per-tier `max_pixels` budget (`PIXEL_BUDGETS` triage<deep, never hardcoded Claude cap), dims rounded down to multiples of 32 (2×patch-16), never upscales; tracks captured/sent `ratioX/ratioY` and `rescalePoint`/`rescaleRect` map model-space coords back to captured space (1:1 element refs). Pixel resampling (sharp) is the worker seam; math is pure + tested.
- [x] #17 - Capture: full-page tiling (viewport-height, 15% overlap, labels) -> done: pure `tiling.ts` — `planTiles` tiles a long page into viewport-height, labeled (`segment k/n, y=Ypx`), 15%-overlapping, bottom-aligned send tiles; `planCaptureSegments` splits pages past the ~16384 device-px raster ceiling into non-overlapping capture segments. Each tile then fits the budget via #16. Rastering is the worker seam; geometry is pure + tested.
- [x] #18 - Capture: DOM geometry map (selector/role/rect) -> done: pure `geometry.ts` — `serializeGeometry` filters to landmarks (nav/headings/buttons/inputs + landmark roles), builds a stable selector (id > data-testid > extractor cssPath > tag), normalizes implicit/explicit roles, carries the animated flag, and `animatedExclusions` surfaces the phash exclusion list (#15). `getBoundingClientRect` extraction is the #11 worker seam; selection/serialization is pure + tested.
- [x] #19 - Capture: a11y tree + computed-style deterministic checks (contrast/overflow/touch-target) -> done: pure `checks.ts` in `@engine/capture` — WCAG relative-luminance contrast ratio + AA threshold (relaxed for large text), horizontal-overflow detection (contentWidth vs container), and sub-44px touch-target detection, over captured computed styles + geometry. Emitted as factual `DeterministicFinding`s for the prompt (facts, not questions); stays silent when a color can't be parsed (no guessing). Browser-side extraction is the #11 worker seam; the computations are pure + fully tested.
- [x] #20 - Capture: console-error + failed-request page-health footnote -> done: pure `page-health.ts` in `@engine/capture` — `buildPageHealth` aggregates raw console events (counts level=error only) + failed requests into the `PageHealth` summary (kept OUT of the design findings set), `pageHealthFootnote` renders the delivery footnote (null when clean, includes the #15 unstable flag). Browser event listeners are the worker seam; aggregation is pure + tested.
- [~] #21 - Capture: dark-mode capture (fresh context, pre-goto colorScheme) -> skipped: live-browser (fresh Playwright context + `colorScheme: dark` pre-goto); can't run in CI. Seam = `captureInSandbox` stub (the `CaptureContext.darkMode` flag already exists). Revisit on real capture infra.
- [~] #22 - Capture: Firecracker-microVM-on-Fly capture sandbox -> skipped: live infra — requires a Fly Machines KVM host + Firecracker + a built guest image; cannot run in CI. `captureInSandbox` stub is the seam; the deterministic post-capture logic (#15/#16/#17/#18/#19/#20) and the egress/storageState policy (#24/#25) are implemented as pure modules the real microVM worker will call.
- [x] #23 - Capture: SPIKE build-vs-buy isolated-browser fleet (Firecracker vs Browserbase) -> resolved: BUILD. Decision recorded in ARCHITECTURE §4 + TRD §4 + the decisions table (E3): Firecracker microVM per job on Fly Machines, not a managed vendor — KVM-grade isolation for hostile PR code AND network-layer egress control are hard requirements, and a managed vendor would also see customer-PII screenshots with no in-VPC path. COGS/warm-pool-idle noted (cold-start MVP; warm-pool #77 deferred); reversal path = both options sit behind `captureInSandbox`. No code (spike).
- [x] #24 - Capture: egress policy — deny RFC-1918/link-local/metadata, allow public assets -> done: pure `egress.ts` — `isPrivateOrReservedIp` denies RFC-1918 + 127/8 + 169.254/16 (incl. 169.254.169.254 metadata) + IPv6 ::1/ULA/link-local/v4-mapped; `evaluateEgress(ip, host, opts)` applies that to the *resolved* IP (worker re-resolves to defeat rebinding) plus a blocklist (exact + CIDR) and an optional domain allowlist; `DomainBudget` enforces per-domain request + byte caps. Kernel nftables enforcement is #73; the policy decision is pure + tested.
- [x] #25 - Capture: storageState injection (origin-scoped, KMS-decrypt in-VM, off on fork PRs) -> done: pure `storage-state.ts` — `decideStorageState` injects only on non-fork PRs with a configured secret and lists fork-PR routes `not-reviewed-fork-PR`; `scopeCookiesToOrigin` keeps only exact-origin cookies, dropping org-wide parent-domain SSO cookies. KMS decrypt-in-VM uses #7 (`openRepoSecret`) at the worker seam; the gating + origin-scoping is pure + tested.
- [~] #73 - Capture: egress enforcement (nftables in-VM) + DNS-rebind/metadata tests -> skipped: kernel-level enforcement (nftables in the guest network namespace) needs a live microVM (#22); can't run in CI. The pure policy decision + rebind re-check seam is implemented in #24 (`evaluateEgress` applied to the re-resolved IP); the nftables ruleset + live rebind/metadata tests land on real capture infra.

## EM2 · Context & critique

- [x] #56 - Context: Tailwind v3 resolveConfig in sandboxed worker -> done: `resolveTailwindV3Tokens` runs Tailwind's own `resolveConfig` (never static-AST-parse → preset/required defaults preserved) and flattens the resolved theme (colors/spacing/fontSize/fontFamily/fontWeight/lineHeight/borderRadius/screens/boxShadow) into the shared `TokenMap`; returns null on throw so the caller degrades to CSS-property extraction (#58). The untrusted `tailwind.config.{js,ts}` LOAD runs in the capture isolation class (#22 seam, [~]); the resolve+flatten is pure + tested (incl. DEFAULT-collapse, fontFamily stacks, fontSize tuples, throw→null).
- [x] #57 - Context: Tailwind v4 @theme/@config via PostCSS -> done: `extractTailwindV4` parses CSS-first v4 config with PostCSS — collects `@theme { --* }` custom properties as tokens and surfaces a `@config "..."` directive path for v3-style resolution (#56). Pure + tested.
- [x] #58 - Context: CSS custom-properties extraction -> done: `extractCssCustomProperties` in `@engine/context` parses global stylesheets with PostCSS and collects `--*` declarations from `:root`/`html` (base) + theme-scoped blocks (`[data-theme=...]`, `.dark`/`.light`, `@media (prefers-color-scheme: ...)`), keyed by theme; component-scoped custom props on other selectors are ignored (not design tokens). Feeds the context block (#63). Pure + tested.
- [x] #59 - Context: tokens.json (W3C / Style Dictionary) parser -> done: new `@engine/context` package + shared `TokenMap` model (`mergeTokens` later-source-wins, `sortTokens` deterministic) and `parseTokensJson` handling both W3C (`$value`/`$type`) and classic Style Dictionary (`value`) nested shapes, skipping `$`-metadata, dotted-name flattening, composite tokens serialized deterministically. Feeds the context block (#63). Pure + tested.
- [x] #60 - Context: component-library detection -> rubric addenda -> done: `detectComponentLibraries` in `@engine/context` detects shadcn/ui (cva signature), Radix, MUI, Chakra, Mantine from package.json deps/devDeps/peerDeps and maps each to a library-specific rubric addendum appended to the critique prompt; no-op when none detected. Pure + tested.
- [x] #61 - Context: .designreview.yml brand block extraction -> done: `extractBrandBlock` in `@engine/context` parses `.designreview.yml` with the `yaml` lib, normalizes the `brand:` block (description/tone/audience + do/dont, accepting alt don't spellings + string-or-array lists), and returns null when the block is absent/empty/invalid; `brandDimensionEnabled` gates the brand dimension so it's suppressed entirely without a stated brand. Pure + tested.
- [x] #62 - Context: diff->route mapping (framework page-files MVP + import-graph v1.5) -> done: `pageFileToRoute` maps Next.js App Router (`app/.../page.ext`, route groups dropped, dynamic `[slug]` kept, private `_`/parallel `@` skipped) and Pages Router (`pages/*.ext`, index/_app/api excluded) files to routes; `mapDiffToRoutes` applies config overrides (`always`, `map`, `maxPerPr`), dedupe + sort. MVP shipped; the v1.5 import-graph path is intentionally deferred behind measured need. Pure + tested.
- [x] #63 - Context: deterministic context-block serialization + content-hash cache invalidation -> done: `buildContextBlock`/`serializeContextBlock` assemble all extractors (tokens, brand, component libs, uiDnaVersion, routes) into ONE byte-stable block — recursively sorted keys, sorted arrays, no timestamps — with a sha256 `contentHash` cache key (invalidates on any token/config change, never wall-clock TTL) and a `CONTEXT_VERSION` stamp folded into the hash. Tested: byte-identical across input reorderings, hash changes on token/brand change. This is the prefix-cache anchor (#34).
- [x] #26 - Critique: critique() interface + per-pass model abstraction -> done: `@engine/critique` rebuilt — one `critique(images, context, options, deps?)` entry point routing through a swappable `ModelClient` interface (DashScope/self-host/mock backends); `DEFAULT_PASS_MODELS` (triage=qwen3-vl-flash non-thinking, deep=qwen3-vl-plus Thinking) + `resolvePassModel` make model id/backend/thinking per-pass config-driven; a model swap (Qwen3-VL<->Claude, DashScope<->self-host) is config-only with no call-site change; result stamped with the resolved model (#68). `MockModelClient` is the default stand-in (no live model in tests; #27 wires real DashScope). Tested incl. swap + injected-client routing.
- [x] #27 - Critique: model SDK streaming + Thinking-checkpoint wiring (OpenAI-compatible) -> done: `DashScopeModelClient` streams against the DashScope OpenAI-compatible endpoint (OpenAI-SDK shape, not @anthropic-ai/sdk), splits `reasoning_content` (thinking) from `content`, reports `cached_tokens` (#34), and threads an AbortSignal into every call (ModelCallOptions; supersession #66). Deep pass sets `enable_thinking` + temp ~0.6; `json_object` is set ONLY on the non-thinking coercion call (thinking⊥json, two-step #29). `createOpenAICompatibleCreate` adapts a real OpenAI client (same client serves self-host vLLM/SGLang #76 by endpoint). The streaming `create` is injected → unit-tested against a fake stream (no live model). MockModelClient honors the signal too.
- [x] #28 - Critique: triage pass (qwen3-vl-flash) + phash-vs-baseline short-circuit -> done: `runTriage` short-circuits (no model call) when every captured route's perceptual hash matches the cached baseline (#15 `hashesWithin`), returning a one-line "no design changes" result; otherwise runs `qwen3-vl-flash` over the above-fold crops and emits `{needsDeepReview, suspectRoutes, obviousBreakage}` (Zod-parsed), folding in the deterministic overflow/breakage facts (#19) which force a deep review. Fails open (review when the model output is unparseable). Tested: short-circuit (no call), model decision, deterministic-breakage merge.
- [x] #29 - Critique: deep pass (qwen3-vl-plus Thinking, <=3 concurrent, two-step JSON) -> done: `runDeepPass`/`critiqueRouteTwoStep` — one request per route via `mapWithConcurrency` (cap 3), Thinking checkpoint. Two-step on DashScope: (1) Thinking critique (prose+reasoning, no response_format), (2) non-thinking `json_object` coercion of that prose → Zod parse (#31); `max_tokens` is never set (ModelRequest has no such field). The thinking message puts the stable context block (#63) under the prefix-cache boundary and interleaves tiled labeled images (#17) + deterministic facts (#19) + feedback digest. Never emits a partial: a route whose coercion fails Zod yields null output, not malformed JSON. Self-host one-call guided decoding documented (#76). Tested: two-step ordering, no-partial, concurrency cap, order preservation.
- [x] #30 - Critique: system prompt + 8-dimension rubric + anti-hallucination clause -> done: `buildSystemPrompt` in `@engine/critique` emits the frozen, versioned (`SYSTEM_PROMPT_VERSION`) prompt over the 8 `@engine/types` dimensions (visual_hierarchy/spacing/typography/color_contrast/consistency/responsiveness/accessibility/brand); `activeDimensions` suppresses `brand` entirely when no brand block (#61); the anti-hallucination clause requires every finding to cite a captured image segment + element_ref, forbids hover/focus/active/animation findings, and pushes uncertainty into lower confidence; component-library addenda (#60) appended. Pure + tested.
- [x] #31 - Critique: Zod output schema + json_object/guided-decoding validation -> done: `FindingSchema`/`CritiqueOutputSchema` (zod) match the §6.4 contract (8 dims/severity/viewport/grade enums kept in sync with `@engine/types` via `satisfies`); `parseCritiqueOutput` JSON-parses + Zod-validates the model output so delivery never parses prose (invalid JSON / wrong enums / bad field types → typed error); `schemaInstruction` is the in-prompt schema description for the DashScope `json_object` path and contains the literal "JSON" keyword (server-validated, per the 2026-06-18 research note). `critique()` now routes output through it. Self-host uses vLLM guided decoding (#76). Tested.
- [x] #32 - Critique: post-parse validation gate + hallucination metric -> done: `hallucinationGate` in `@engine/critique` — the core drop-and-count: clamps confidence into [0,1] (non-finite→0), drops any finding whose `route` wasn't captured or whose `elementRef` isn't a key in the geometry map (#18; elementRef=null allowed), and returns `hallucinationDrops` as the model-hallucination metric feeding eval + the gated SLO (#72) / Grafana (#9). `critique()` runs the gate on the validated output (captured routes from the images, geometry selectors via deps). Tested: route drop, element_ref drop, null-ok, no-geometry skip, confidence clamp.
- [x] #33 - Critique: post-filter (>=0.55, dedupe, cap 1 blocker + 6) -> done: `postFilter` in `@engine/critique` — drops findings below confidence 0.55, dedupes by `elementRef`+`dimension` across viewports (keep highest confidence), and caps at 1 blocker + 6 other findings with deterministic ordering (severity, confidence desc, route/dimension/elementRef tiebreak). Wired into `critique()` after the #32 gate. Tested incl. determinism.
- [x] #34 - Critique: prefix-cache layout (stable context block) + cache-hit telemetry + byte-identical test -> done: `cachePrefix(systemPrompt, contextBlock)` is the stable cacheable prefix the deep pass (#29) puts first; volatile content (route/images/geometry/feedback) follows it, so prefix caching (DashScope context cache / vLLM auto prefix cache) reuses it across reviews of the same repo state. `cachedInputTokens`/`isCacheHit` read the model-reported `cached_tokens` (#27) for telemetry (#9). Byte-identical test: same context block → identical prefix; changed tokens → different prefix; volatile content absent from the prefix. Explicit `cache_control` (10% vs 20%, 1024-tok floor) noted as the later lever per the 2026-06-18 research.
- [x] #35 - Critique: free-tier deep-pass model swap (qwen3-vl-flash / Instruct) -> done: `passModelsForTier(tier)` returns per-pass overrides — free tier routes the deep pass to `qwen3-vl-flash` (non-thinking Instruct) to cut cost, paid keeps `qwen3-vl-plus` Thinking. Config-only swap via the #26 abstraction (no call-site change); `critique()`/`runDeepPass` accept it through `passModels`. Tested: paid vs free resolution + end-to-end model id via critique().
- [x] #69 - Critique: max_pixels image-token budget enforcement in adapter -> done: `critique()` sets the per-tier Qwen3-VL `max_pixels` budget (from #16's `PIXEL_BUDGETS`, triage<deep) on every `ModelRequest`; the DashScope client forwards it via `extra_body.max_pixels`. No Claude-era pixel/token constants on the path; the captured/sent coordinate rescale lives in #16 (`rescaleRect`) and the engine grounds on element_ref from the geometry map (#18) rather than VLM pixels. Tested: per-tier budget reaches the request.
- [x] #70 - Critique: confidence-ceiling propagation (unstable page caps findings) -> done: `applyConfidenceCeiling` caps every finding's confidence at the ceiling when the capture was flagged visually unstable (#15's `UNSTABLE_CONFIDENCE_CEILING`); `critique()` applies it (from `CritiqueOptions.confidenceCeiling`) before the post-filter and sets `validation.captureUnstable = true`. The 0.6 ceiling sits above the 0.55 floor so capped findings still surface, just with lowered trust. Pure + tested.

## EM3 · Eval & quality gate

- [x] #44 - Eval: synthetic-canary generator -> done: new `@engine/eval` package + `generateCanaries(baseline, opts)` — a programmatic defect injector (mutated_token→color_contrast, broken_breakpoint→responsiveness/major, swapped_font→typography) producing canary specs across routes × defects × variants with **ground truth known by construction** (dimension/route/minSeverity). Deterministic + stable ids (reproducible frozen set); scales into the hundreds via `variantsPerCombo`. The mutation describes what to change; rendering is the capture worker's job (#11). Pure + tested. (Regression gate #47 asserts recall against these.)
- [ ] #45 - Eval: 150-PR golden set + labeling tooling
- [ ] #46 - Eval: metrics (precision/recall, blocker recall, nit precision, weighted-kappa + CIs)
- [ ] #47 - Eval: regression gate (hard on canary recall, monitor humans, offline batch)
- [ ] #48 - Eval: quality gate — critique clears §10 golden-set + canary bar on frozen capture set
- [ ] #49 - Eval: weekly production injected-defect canary
- [ ] #50 - Eval: public benchmark publication
- [ ] #71 - Eval: model/prompt registry + CI eval-gated promotion + rollback
- [ ] #72 - Eval: hallucination-drop + capture-instability SLOs coupled to the gate

## EM4 · Data moat & learning

- [ ] #37 - Data: Postgres schema (findings / feedback / rater_permission)
- [ ] #38 - Data: explicit feedback signals (thumbs + /ignore)
- [ ] #39 - Data: implicit signal — suggestion string-match (drop touched-element heuristic)
- [ ] #40 - Data: in-loop recheck labeling
- [ ] #41 - Data: per-repo memory digest (<=600 tok) -> deep-pass suffix
- [ ] #42 - Data: rater-permission down-weighting
- [ ] #43 - Data: preference-dataset export (image/context/finding/verdict tuples)
- [ ] #74 - Data: tenant training-consent + PII scan for training data
- [ ] #75 - Data: DVC-versioned preference-dataset export on R2

## EM5 · Security & residency

- [ ] #51 - Security: SSE-KMS at rest + retention (0 default / 30d paid)
- [ ] #52 - Security: SSRF hardening + tests (DNS rebind, metadata)
- [ ] #53 - Security: prompt-injection defenses (delimiter + rendered-text canaries + schema)
- [ ] #54 - Security: GDPR/DPA + data-processor artifacts
- [ ] #55 - Security: SOC 2 via Vanta (enterprise)

## EM6 · Scale & owned model (deferred)

- [ ] #76 - Scale: self-host vLLM/SGLang serving + GPU warm-pool autoscale (act-2)
- [ ] #77 - Capture: warm-pool manager (snapshot/UFFD)
- [ ] #78 - Owned model (act-3): ORPO fine-tune training + eval-gated shadow promotion
- [ ] #79 - Enterprise: in-VPC self-hosted engine + capture residency path
- [ ] #80 - Scale hardening (deferred): webhook callback, json_schema coercion, shadow/canary, per-tenant LoRA
